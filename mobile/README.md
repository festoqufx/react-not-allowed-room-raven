# NotAllowedRoom mobile

Expo SDK 56 companion app. The shell follows system light/dark mode and can cycle system → light → dark.

```bash
npm install
npx expo start
```

Use Node.js 24.x. See the [Expo SDK 56 docs](https://docs.expo.dev/versions/v56.0.0/).
